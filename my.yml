eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
server:
  port: 8081	
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lizhengsb/git-.git
          username: lizhengsb
          password: lw634672
